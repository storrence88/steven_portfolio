%h1 Portfolio Items

- portfolio_items.each do |item|
  %p
    = link_to "#{item.title}", portfolio_path(item)
  %p
    = item.subtitle
  %p
    = item.body
  %img{src: "#{item.thumb_image}"}
  = link_to 'Delete Portfolio Item', portfolio_path(item), method: :delete, data: {confirm: 'Are you sure?'}